# Project Title: JAVASCRIPT PROOF OF CONCEPT

## Description
This project is comprised of PoC written on JS, which helps to understand core JavaScript concepts.

## Prerequisites
List any required software or tools (e.g., Node.js, a specific browser, `npm`).
node.js
npm

## List of PoC
Clearly state what specific idea, hypothesis, or function this PoC aims to validate.
*   [ ] Objective 1: e.g., Demonstrate real-time data fetching from an external API.
*   [ ] Objective 2: e.g., Test a new UI library's compatibility with our existing stack.

## Dependencies
http-server : sets up local server 
nodemon : restarts local server automatically on any update made in the code

## Commands Used 
npm -init : to initializes the NPM project. It helps to install, update and manage downloads of dependencies of your project.

npm install : to install the necessary dependencies.

npx dependencyName : Designed for executing packages directly without requiring a permanent installation. It's particularly useful for      running  one-time commands, scaffolding new projects, or executing tools that are not project-specific dependencies.

npx http-server . -o -p 9999 : to run/initialize the http-server(dependency) at 9999 port

